<Window x:Class="RemoteNetSpy.MemoryViewWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:RemoteNetSpy" xmlns:controls="clr-namespace:RemoteNetSpy.Controls"
        mc:Ignorable="d"
        Style="{DynamicResource CustomWindowStyle}"
        Title="MemoryViewWindow" Height="450" Width="620">
    <Window.Resources>
    <!-- Style for Buttons -->
    <Style TargetType="Button" BasedOn="{StaticResource darkThemeButtonStyleKey}">
        <Setter Property="Background" Value="{x:Null}"/>
        <Setter Property="BorderBrush" Value="{x:Null}"/>
    </Style>
    </Window.Resources>
    <Grid>
        <controls:MemoryViewPanel x:Name="memoryViewPanel"/>
    </Grid>
</Window>
