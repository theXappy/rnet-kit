<Window x:Class="RemoteNetSpy.Controls.DragDropPlaygroundSubsystem.PrimitiveInputWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:RemoteNetSpy.Controls.DragDropPlaygroundSubsystem"
        mc:Ignorable="d"
        Style="{DynamicResource CustomWindowStyle}"
        Title="Enter Value" Height="100" Width="300" WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Window.Resources>
        <!-- Style for Buttons -->
        <Style TargetType="Button" BasedOn="{StaticResource darkThemeButtonStyleKey}">
            <Setter Property="Background" Value="{x:Null}"/>
        </Style>
    </Window.Resources>
    <StackPanel Margin="10">
        <TextBox x:Name="InputTextBox" Margin="0 0 0 10" />
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="OK" Width="75" Margin="0 0 10 0" IsDefault="True" Click="Ok_Click"/>
            <Button Content="Cancel" Width="75" IsCancel="True"/>
        </StackPanel>
    </StackPanel>
</Window>
